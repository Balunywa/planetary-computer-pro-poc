# Base Python environment the workstation installs so the official Microsoft
# Planetary Computer Pro notebooks run out of the box (GeoCatalog_Tutorial.ipynb
# and create-stac-items.ipynb). The Aurora storm-impact app has its own, heavier
# requirements.txt inside the cloned repo, which setup.ps1 installs best-effort.
jupyter
notebook
ipykernel
pystac-client
azure-identity
azure-storage-blob
requests
pillow
azure-planetarycomputer
